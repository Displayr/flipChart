name: Build flipChartPrivate

on:
  workflow_dispatch:
  push:

jobs:
  build:
    name: Build flipChartPrivate
    permissions:
      checks: write
      contents: read
    uses: Displayr/nixr-public/.github/workflows/build-flake-installable.yaml@master
    with:
      installable-path: pkgs.x86_64-linux.rPackages.flipChartPrivate
      build-args: --override-input displayr-flipChartPrivate .
      installable-name: flipChartPrivate
    secrets: inherit